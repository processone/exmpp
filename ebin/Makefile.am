# $Id$

beamdir =  $(ERLANG_INSTALL_LIB_DIR_exmpp)/ebin

beam_DATA = exmpp.app\
	    exmpp.beam exmpp_xml.beam exmpp_xmlstream.beam\
	    exmpp_jlib.beam exmpp_factory.beam\
	    ejabberd_zlib.beam iconv.beam sha.beam stringprep.beam\
	    tls.beam xml.beam

CLEANFILES = $(beam_DATA)
