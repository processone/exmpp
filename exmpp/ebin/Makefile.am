# $Id$

beamdir =  $(ERLANG_INSTALL_LIB_DIR_exmpp)/ebin

beam_DATA = exmpp.beam exmpp_xml.beam exmpp_xmlstream.beam exmpp_jlib.beam\
	    iconv.beam sha.beam stringprep.beam tls.beam

CLEANFILES = $(beam_DATA)
